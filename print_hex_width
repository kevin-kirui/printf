#include "main.h"

int main() {
	handle_flags((va_list)NULL, 7, 'd');  // Binary: 111 (All flags: +, space, #)

	return 0;
}

void handle_flags(va_list val, int flags, char specifier) {

	printf("Flags: %d, Specifier: %c\n", flags, specifier);
}
